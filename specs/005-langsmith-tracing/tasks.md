---

description: "Задачи: трассировка вызовов модели"
---

# Tasks: Трассировка вызовов модели в LangSmith

**Tests**: включены. Главное свойство фичи — «не ломает продукт» — проверяется только
прогоном: без ключа, с неверным ключом, при недоступной службе.

## Phase 1: Setup

- [X] T001 Добавить `langsmith` в зависимости в `pyproject.toml`
- [X] T002 [P] Описать переменные трассировки в `.env.example` с пояснением, что без ключа она молча выключена

## Phase 2: Foundational

- [X] T003 Обернуть вызов модели в `src/core/llm.py`: промпт, ответ, токены, длительность
- [X] T004 Добавить в запись признак окружения и ИНН контрагента в `src/core/llm.py`
- [X] T005 Гасить любые ошибки трассировки в `src/core/llm.py` — наблюдение не может стать причиной отказа

## Phase 3: US1 — видно, что ушло и что вернулось (P1)

- [X] T006 [US1] Передавать ИНН контрагента в вызов из `src/api/agent/loop.py`
- [X] T007 [US1] Проверить на живом ключе, что запись появляется с промптом, ответом и токенами

## Phase 4: US2 — трассировка не ломает продукт (P1)

- [X] T008 [US2] Создать `tests/test_tracing.py`: без ключа трассировка выключена, вызов проходит
- [X] T009 [US2] Добавить в `tests/test_tracing.py` проверку, что ошибка трассировки не долетает до вызывающего
- [X] T010 [US2] Проверить вручную три случая: ключа нет, ключ неверный, служба недоступна

## Phase 5: US3 — понятно, откуда вызов (P2)

- [X] T011 [US3] Различать локальное окружение и сервер в `src/core/llm.py` по уже существующему признаку

## Phase 6: Polish

- [X] T012 Прокинуть `LANGSMITH_*` из секретов в `.github/workflows/deploy.yml`
- [X] T013 [P] Описать включение и выключение трассировки в `deploy/PREVIEW-DEPLOY.md`
- [X] T014 Прогнать `ruff` и `pytest` из `.github/workflows/ci.yml`

## Dependencies

Фундамент блокирует истории. US1, US2, US3 независимы: US1 про содержание записи,
US2 про устойчивость, US3 про различение окружений.

## Implementation Strategy

MVP — Setup, фундамент и US1: записи появляются и в них видно главное.
US2 резать нельзя: без неё трассировка становится новым способом уронить продукт.
