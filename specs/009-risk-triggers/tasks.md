# Tasks: Триггеры из данных и словарь полей

**Feature**: `009-risk-triggers` | **Spec**: [spec.md](spec.md) | **Plan**: [plan.md](plan.md)

Замеры, обосновывающие пороги и отказы, — [research.md](research.md).

## Phase 1: Словарь полей

- [X] T001 Написать `scripts/build_field_dictionary.py`: разобрать таблицы `data/GetFullReportResponse.md` в пары «путь → описание, раздел», обойти набор и посчитать заполненность каждого пути
- [X] T002 Дописать в `scripts/build_field_dictionary.py` статьи для 11 путей, которых нет в спецификации, с пометкой `origin: own`; зафиксировать расхождение `cofounders[].active` против `isActive`
- [X] T003 Сгенерировать `src/core/fields.json` и закоммитить — `data/` лежит вне репозитория и в образ не попадает
- [X] T004 [P] Написать `src/core/fields.py`: чтение словаря один раз на процесс, `label(path)` и `describe(paths)` для подписи источников
- [X] T005 [P] Тесты `tests/test_fields.py`: покрытие всех используемых продуктом путей, происхождение описаний, расхождение спецификации с данными зафиксировано

## Phase 2: Триггеры (User Story 1, P1)

- [X] T006 Написать `src/core/triggers.py`: `Trigger`, девять правил из research.md, каждое чистой функцией от записи
- [X] T007 [US1] Провести триггеры в `src/core/report.py`: поле `triggers` в `Report`, порядок по `weight`
- [X] T008 [P] [US1] Тесты `tests/test_triggers.py`: логика каждого правила на синтетических записях
- [X] T009 [US1] Тест на настоящем наборе: частоты из research.md закреплены числами, 74% компаний без единого триггера, МАКСМАРКЕТ даёт пять
- [X] T010 [US1] Отвергнутые кандидаты записаны тестом: «обязательства больше активов» срабатывает один раз и это артефакт округления (актив равен пассиву в 388 отчётах из 396), «убыток» и «свежие производства» дублируют коды источника
- [X] T011 [US1] `src/web/src/components/TriggersBlock.tsx`: список с объяснением, значениями и ссылкой на раздел; пустой случай — словами
- [X] T012 [US1] Провести `triggers` через `src/web/src/types.ts` и `App.tsx`, стили в `styles.css`

## Phase 3: Подписи источников словами (User Story 3, P3)

- [X] T013 [US3] `src/core/charts.py`: `source` собирается из словаря по путям, а не пишется строкой
- [X] T014 [US3] Проверить, что ни одна подпись на экране не содержит путей: тест на всех графиках всех 200 компаний

## Phase 4: Порядок по вопросу (User Story 2, P2)

- [X] T015 [US2] Намерения в `src/core/triggers.py`: `Intent`, четыре намерения из `docs/roles_situations.md`, узнавание по словам
- [X] T016 [US2] `order_for(triggers, question)`: переставляет по тегам намерения, состав не меняет
- [X] T017 [P] [US2] Тесты: два разных вопроса дают разный порядок при одинаковом составе; неузнанный вопрос оставляет порядок по значимости; ни один триггер не исчезает
- [X] T018 [US2] Применить порядок в `/chat` и `/chat/stream` в `src/api/main.py`

## Phase 5: Агент видит триггеры

- [X] T019 Блок «Противоречия в данных» в `src/api/agent/prompt.py`, формулировками «обратите внимание», без вердикта
- [X] T020 Тест: триггеры попадают в промпт, формулировки не содержат вердиктов уровня «не заключать договор»

## Phase 6: Проверка

- [X] T021 Прогнать `quickstart.md` руками на трёх компаниях
- [X] T022 Полный прогон тестов и линтеров, сборка фронта
- [X] T023 Выкатить и проверить на живом сервере

## Зависимости

```
T001 → T002 → T003 → T004 → T005        словарь
T003 ─────────────────────→ T013 → T014 подписи (нужен словарь)
T006 → T007 → T008..T010 → T011 → T012  триггеры
T007 ─────────→ T015 → T016 → T017 → T018  порядок (нужны триггеры)
T007 ─────────→ T019 → T020             промпт
всё → T021 → T022 → T023
```

## Порядок сдачи

**MVP — фазы 1–2:** словарь и триггеры на экране. Уже закрывает главную
претензию: пользователь видит противоречия, не читая разделы.

Фаза 3 маленькая и закрывает прямую просьбу кейсодателя про технические термины.

Фазы 4–5 расширяют, но без них продукт работает.
