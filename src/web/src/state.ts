import type { SectionState } from './types';

/**
 * Состояние раздела: цвет и слова.
 *
 * Одна таблица на весь продукт. Раньше цвет точки, цвет плашки и её текст жили
 * тремя списками в `ReportSection`, и любое новое место показа состояния —
 * строка разделов, карточка в ленте — заводило четвёртый.
 *
 * Серый у «нет данных» и «не применимо» общий намеренно: оба означают
 * «оценить по этому критерию нельзя», а различие между ними — в словах.
 * Красить их по-разному значило бы сказать цветом то, чего цвет не выражает.
 */
export const STATE_COLOR: Record<SectionState, string> = {
  signal: '#ec2d20',
  filled: '#0d9336',
  empty: '#9a9da4',
  not_applicable: '#9a9da4',
};

export const STATE_LABEL: Record<SectionState, string> = {
  signal: 'Есть на что обратить внимание',
  filled: 'Значимых сигналов нет',
  empty: 'Недостаточно данных',
  not_applicable: 'Не применимо',
};
