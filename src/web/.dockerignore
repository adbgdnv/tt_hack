# Контекст сборки — сам каталог src/web. node_modules ставит npm ci внутри образа,
# dist собирается там же: копировать хостовые копии и дольше, и опаснее
# (в них попадает окружение хоста).
node_modules/
dist/
*.tsbuildinfo
.DS_Store
