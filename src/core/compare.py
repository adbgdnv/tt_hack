"""Сравнение нескольких контрагентов в одной сессии.

Кейсодатель на Q&A разрешил и одновременно ограничил этот сценарий:

- «Потребность есть именно чтобы у агента была тулза сравнивать между собой
  контрагентов, находить оптимального среди какой-то группы» — сценарий целевой.
- «Скорее **не ранжирование**. Нужно вывод — с кем лучше не общаться. Ранжирование
  в виде какого-то скора **не требуется**» — рейтинговой таблицы на выходе быть не должно.

Жёсткого предела на число компаний нет. Ориентир кейсодателя: под капотом может
анализироваться много, пользователю показывается несколько.

Батч из десяти отобранных отчётов — 2 519 токенов, укладывается в минутный лимит Groq.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass
class Verdict:
    """Итог по одному контрагенту внутри группы. Без числового скора — намеренно."""

    inn: str
    name: str
    recommendation: str  # «обратить внимание» / «уточнить» / «выглядит чисто»
    reasons: list[str]  # каждая причина прослеживается до поля отчёта
    gaps: list[str]  # чего в данных нет — проговариваем, а не умалчиваем


def compare(reports: list[dict]) -> list[Verdict]:
    """Разбирает группу контрагентов и отмечает, с кем стоит быть осторожнее."""
    raise NotImplementedError


def shortlist(reports: list[dict], limit: int = 3) -> list[Verdict]:
    """Оставляет несколько контрагентов, на которых стоит посмотреть в первую очередь."""
    raise NotImplementedError
